#include <stdio.h>
/**
* main - print a string
* Return: Description of the returned value
*/
int main(void)
{
printf(sizeof(char)\n);
printf(sizeof(int)\n);
printf(sizeof(long)\n);
printf(sizeof(long long int)\n);
printf(sizeof(float)\n);
return (0);
}
