#include "main.h"
/**
* print_square  - square line
* Return: square line
* @n: numbers
*/
void print_square(int size)
{
int x, y;
for (x = 0; x <= size; x++)
_putchar('\n');
for (y = 0; y <= size; y++)
_putchar(35);
}
