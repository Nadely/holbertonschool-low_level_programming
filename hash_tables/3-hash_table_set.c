#include "hash_tables.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * key_index - that adds an element to the hash table
 * @ht: hash table
 * @key: Keys
 * @value: new value from keys
 * Return: that adds an element to the hash table
 */
int hash_table_set(hash_table_t *ht, const char *key, const char *value)
{}
