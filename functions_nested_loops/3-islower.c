#include "main.h"
/**
* _islower - lowercase character
* Return: lowercase character
*/
int _islower(int c)
{
int az;
for (az = 'a'; az <= 'z'; az++) 
if (az = 'c')
{
_putchar(1);
}
else
{
_putchar(0);
}
return (0);
}
