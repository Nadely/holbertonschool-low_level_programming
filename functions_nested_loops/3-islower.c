#include "main.h"
/**
* _islower - lowercase character
* Return: lowercase character
*/
int _islower(int c)
{
for (c = 'a'; c <= 'z'; c++)
{
if (c == 'c')
{
return(1);
}
else
{
return(0);
}
}
return (0)
}
