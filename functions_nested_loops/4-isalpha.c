#include "main.h"
/**
 * _isalpha - lowercase or not
 * Return: lowercase or not
 * @c: isalpha
 */
int _isalpha(int c)
{
	if (c >= 65 && c <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	{
		return (0);
	}
}
