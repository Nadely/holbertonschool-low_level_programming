#include "main.h"
/**
* print_last_digit - last number
* Return: last number
* @x: last
*/
int print_last_digit(int x)
{
int n = x % 10;
if (x < 0)
{
return (-n * (n));
_putchar (n);
}
else
{
_putchar(n);
return (0);
}
{
return (0);
}
}
