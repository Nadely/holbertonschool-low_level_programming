#include "main.h"
/**
* *_strchr - locates
* Return: locates
* @s : pointer
* @c: character
*/
char *_strchr(char *s, char c)
{
for (s = 0; s != '\0'; i++)
{
if (s == c)
return (s);
}
return (NULL);
}
