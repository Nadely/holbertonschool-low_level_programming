#include "main.h"
#include "stdio.h"
/**
 * print_array - Array
 * Return: Array
 * @a: pointer
 * @n: numbers
 */
void print_array(int *a, int n)
{
int l;
for (l = 0; l[a] != '\0'; l++)
if (l < n && n >= 0)
printf("%d, ", l[a]);
{
printf("\n");
}
}
