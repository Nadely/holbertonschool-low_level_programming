#include "main.h"
/**
 * _isupper - uppercase character
 * Return: uppercase character
 * @c: letter
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	else
		return (0);
	{
		return (0);
	}
}
