#include <stdio.h>
/**
* main - Alphabet in revers
* Return: Alphabet in reverse
*/
int main(void)
{
int za;
for (za = 'a'; za <= 'z'; za++)
if (!(za = 'a' && za = 'z'))
{
putchar(za);
}
{
putchar('\n');
}
return (0);
}
