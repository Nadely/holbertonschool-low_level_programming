#include <stdio.h>
/**
* main - putchar command
* Return: putchar command
*/
int main(void)
{
int az;
for (az = 'a'; az <= 'z'; az++)
if (az = 'e' && 'q' !=0)
{
putchar(az);
}
{
putchar('\n');
}
return (0);
}
