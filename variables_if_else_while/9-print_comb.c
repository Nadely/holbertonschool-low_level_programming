#include <stdio.h>
/**
* main - Numbers must
* Return: Numbers must
*/
int main(void)
{
int numbers;
for (numbers = '0, '; numbers <= '9, '; numbers++)
putchar(numbers);
putchar(',');
putchar(' ');
putchar('\n');
return (0);
}
