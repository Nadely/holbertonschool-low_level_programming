#include <stdio.h>
/**
* main - putchar command
* Return: putchar command
*/
int main(void)
{
int az;
for (az = 'a'; az != 'e'; az != 'q'; az <= 'z'; az++)
{
putchar(az);
}
{
putchar('\n');
}
return (0);
}
